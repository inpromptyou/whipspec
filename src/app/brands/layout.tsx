import type { Metadata } from "next";
export const metadata: Metadata = { title: "Brands - WhipSpec", description: "The parts and products that make builds happen." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
