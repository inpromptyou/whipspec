import type { Metadata } from "next";
export const metadata: Metadata = { title: "Discover Builds - WhipSpec", description: "Browse Australian car builds by style, make, and location." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
