import type { Metadata } from "next";
export const metadata: Metadata = { title: "Shops - WhipSpec", description: "Find the workshops and installers behind the builds." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
