import type { Metadata } from "next";
export const metadata: Metadata = { title: "Advertise - WhipSpec", description: "Reach the most engaged car audience in Australia." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
